From: NodeByte Browser Authors <dev@nodebyte.cn>
Subject: [PATCH 0240] Register nodebyte:// WebUI configs, scheme wiring, print panel entry

v1.4.4 注册缺口补齐（本补丁为 hand-written hook，对内核文件小修改）：
1. scheme 早期注册：RegisterNodeByteScheme()（url::AddStandardScheme，0110 已有实现）；
2. WebUIConfig 统一注册：nodebyte::RegisterNodeByteWebUIConfigs()（0190 组新文件），
   覆盖 login/drop/settings/translate/game/usercenter/office/print 八个主机；
3. 打印入口重定向（提示词 5.11.2「替换原生打印弹窗」的第一阶段）：
   Print(Browser*) 在 NodeBytePrintPanelEnabled 开启时改为打开 nodebyte://print，
   策略/指令关闭后恢复原生打印预览（无需重编译，运行时可切换）。
4. BUILD.gn 引用 nodebyte_resources 目标由 hook 0230 完成（勿重复添加）。

⚠️ 基线：Chromium 128 ——【需核实】以下每处签名与函数名：
   - chrome_web_ui_configs.cc 的注册函数名（RegisterChromeWebUIConfigs）
   - content::WebUIConfigTable 获取方式（GetWebUIConfigTable / WebUIConfigTable::Add）
   - browser_commands.cc 的 Print(Browser*) 签名与 EarlyOut 语义

--- a/chrome/browser/ui/webui/chrome_web_ui_configs.cc
+++ b/chrome/browser/ui/webui/chrome_web_ui_configs.cc
@@
 #include "chrome/browser/ui/webui/chrome_untrusted_web_ui_configs.h"
 #include "chrome/browser/ui/webui/conflicts/chrome_intervention_report_page_ui.h"
+// NodeByte: nodebyte:// 内部页面统一注册（v1.4.4）
+#include "chrome/browser/ui/webui/nodebyte/nodebyte_ui_configs.h"
@@
 void RegisterChromeWebUIConfigs() {
   ...
   RegisterAboutUIConfig();
   ...
+  // NodeByte: nodebyte:// WebUI（登录/设置/Drop/翻译/游戏/办公/打印/个人中心）
+  nodebyte::RegisterNodeByteWebUIConfigs();
 }
--- a/chrome/common/chrome_content_client.cc
+++ b/chrome/common/chrome_content_client.cc
@@
 void ChromeContentClient::AddAdditionalSchemes(std::vector<url::SchemeType>* types? ) {
   // （保持原函数体；在其列表追加处增加）
+  // NodeByte: nodebyte 标准可浏览 scheme（WebUI 由 WebUIConfig 按 host 挂接）
+  additional_standard_schemes.push_back(nodebyte::kScheme.data());  // 需核实变量名
 }
--- a/chrome/browser/ui/browser_commands.cc
+++ b/chrome/browser/ui/browser_commands.cc
@@
 #include "chrome/browser/ui/browser_commands.h"
+
+#include "chrome/browser/nodebyte/nodebyte_constants.h"
+#include "chrome/browser/nodebyte/nodebyte_protocol.h"
+#include "chrome/browser/nodebyte/policy_extend/nodebyte_policy_prefs.h"
+#include "chrome/browser/platform_util.h"
+#include "chrome/browser/ui/browser_navigator.h"
+#include "chrome/browser/ui/browser_navigator_params.h"
@@
 bool Print(Browser* browser) {
   if (!browser)
     return false;
+
+  // NodeByte: 高级打印面板接管（提示词 5.11.2；策略 NodeBytePrintPanelEnabled，
+  // 关闭/指令撤销后回退原生打印预览）。面板内完成多页合一/小册子/缩放/边距/
+  // 页码范围/导出 PDF（本地 pdf-kit 二次处理），随后交给系统打印框架。
+  if (nodebyte::PrintPanelEnabled(browser->profile())) {
+    navigate_params::NavigateParams params(
+        browser, nodebyte::MakeNodeByteUrl(nodebyte::kHostPrint),
+        ui::PAGE_TRANSITION_AUTO_BOOKMARK);
+    params.disposition = WindowOpenDisposition::NEW_FOREGROUND_TAB;
+    navigate_params::Navigate(&params);
+    return true;
+  }
+
   ...  // 原生打印预览路径（保持不变）
 }
--- a/chrome/browser/ui/webui/nodebyte/policy_bridge.h  （占位说明，实现见 0190 组）
+++ b/chrome/browser/ui/webui/nodebyte/policy_bridge.h
@@
+// nodebyte::PrintPanelEnabled(profile)：
+//   1) prefs "nodebyte.directive.forced.NodeBytePrintPanelEnabled"（指令强制）
+//   2) 默认 true（面板为主；关闭即回原生）
+// 实现与 OfficeController::IsSuiteEnabled 同构（office_controller.cc）。
